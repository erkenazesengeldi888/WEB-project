# AI Log

This file discloses where AI assistance was used while building this project, as required by the assignment.

## Assignment 1 — HTML

No AI tools were used. All pages, content and HTML structure were written by the team (Yerkenaz, Aisha, Dilnaz) without AI assistance.

## Assignment 2 — CSS

**Tool:** Claude (Claude Code, model Sonnet 5), used by Aisha.
**Dates:** 2026-09-21 to 2026-09-22.

**What was generated:**

1. `css/base.css` and all three personal stylesheets — `css/Yerkenaz.css`, `css/dilnaz.css`, `css/aisha.css` — were written by Claude at Aisha's request, covering the required selectors, colours/fonts/spacing, the cascade and specificity experiment, Flexbox, Grid, positioning/float/clear, and the three centering techniques, each with explanatory comments. **Yerkenaz and Dilnaz did not write their own stylesheet themselves** — this should be discussed with the instructor, since each student is expected to be able to explain their own file at the defense.
2. Minor HTML edits needed to support the CSS (a floated image added to a paragraph, id/class names, one internal `<style>` block and one inline `style=` per student, wording fixes).
3. Reorganizing `images/` (moving files into the folder, fixing duplicate/wrong file extensions) and fixing image paths across all 8 pages.
4. Generating `sketches/screenshots/checklist.md` from the actual file contents (an early version had a bug that made a few line numbers point inside multi-line CSS comments instead of the real code; this was found and corrected before the final version).
5. Adding real business details taken from the team's own 2GIS listing (address, opening hours, rating) and from facts given directly by Aisha (franchise owner names, branch manager, one unified contact phone number, Instagram handle), replacing an earlier unverified "130+ branches" figure with the confirmed "112 branches" from 2GIS.
6. Shortening the home page text and adding a "Why Espresso Day" feature grid, including a mention of the Espressoday bonus/cashback app.
7. Connecting the local project folder to this GitHub repository (`git init`, `git remote add`, `git reset` onto `origin/main`, commit, push) after Aisha was added as a collaborator.

All AI-generated CSS and HTML was reviewed, rendered and checked page by page by Aisha, who can explain any selector, the specificity experiment, and the layout choices at the defense, as the assignment requires.

*Yerkenaz and Dilnaz: if you also used AI for anything in your own work, add your own entry above with the tool, date, and what it was used for.*
